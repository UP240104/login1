import os


DB_PASSWORD = os.environ["CONTRASEÑA_BD"]


def es_riesgoso (monto):
	return monto > 1000 and monto > 1000


def puede_entrar (usuario):
	if usuario.activo : 
		if not usuario.bloqueado :
			return True
	return False